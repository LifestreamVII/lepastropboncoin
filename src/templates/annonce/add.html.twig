{% extends 'base.html.twig' %}

{% block title %}Créer une annonce{% endblock %}

{% block body %}
    <div>
        <h1>Déposer une annonce</h1>

        <div class="row">
            <div class="col-4">

                {{ include('annonce/form.html.twig', {'button_label': 'Publier'}) }}

                <div>
                    <a href="{{ path('app_annonce') }}" class="btn btn-link">retour a la liste</a>
                </div>
            </div>
        </div>

    </div>
{% endblock %}